// Generated by CoffeeScript 1.6.2
(function() {
  var env, envTags, name, propertyNameRegex, tag, value, _i, _len;

  propertyNameRegex = /^Env\./;

  envTags = [].slice.call(document.getElementsByTagName('meta')).filter(function(tag) {
    return tag.name.match(propertyNameRegex);
  });

  env = {};

  for (_i = 0, _len = envTags.length; _i < _len; _i++) {
    tag = envTags[_i];
    name = tag.name.replace(propertyNameRegex, '');
    value = tag.content;
    try {
      if (!tag.dataset.hasOwnProperty('envNoCoerce')) {
        eval("value = " + value);
      }
    } catch (_error) {}
    env[name] = value;
  }

  window.Env = env;

}).call(this);
